# Data Flow Diagram

User enters natural language query.

Query is sent to Streamlit interface.

Prompt is passed to AI model.

AI generates SQL query.

SQL query is sent to database.

Database returns results.

Results are shown to user.
